# curdOperation

## Technology

Laravel

## Framework

Laravel

## Language

PHP

## Project Requirements

all operation perform
insert data
update data
delete data
display data

## Project Structure

The project must follow the official Laravel conventions.

## Installation

`composer install`
`php artisan key:generate`
`php artisan migrate`
`php artisan serve`

## Important

This repository must contain the complete application source code.

Do not leave placeholder files such as:
- TODO
- Coming soon
- Implement later
- Example only
- Dummy data where real implementation is required

Every requested feature must be implemented in the appropriate framework files.
